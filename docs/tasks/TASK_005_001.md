---
id: TASK_005_001
title: Normalize Authorization Domain Model
type: task
status: done
priority: high
created: 2026-04-23
updated: 2026-04-25
tags:
  - backend
  - authorization
  - casl
  - abac
  - domain
related:
  - EPIC_005
---

# TASK: Normalize Authorization Domain Model

---

## DESCRIPTION

- Summary (1 sentence): Consolidate the canonical authorization vocabulary for roles, scopes, actions, subjects, and permission specs.
- Steps (max 5):
  1. Review the current enums and permission types under `backend/src/core/domain`.
  2. Define the canonical model for `Action`, `Subject`, platform scope, tenant scope, and permission spec shapes.
  3. Align the model with the current auth/session payloads and membership model.
  4. Remove or isolate ambiguous naming that could be confused between platform and tenant authority.
  5. Expose the final canonical types through a small shared export surface.
- Implementation hints:
  - Keep the MVP model small; do not introduce DB-backed policy concepts yet.
  - Make role names and subject names explicit and stable, because they will be used by decorators and guards.
  - Separate request context data from resource attributes so ABAC conditions stay readable.

---

## EXPECTED OUTPUT

| Field              | Value |
| ------------------ | ----- |
| Output Type        | Domain model refactor |
| Output Location    | backend/src/core/domain/ |
| Output Description | A canonical, minimal authorization vocabulary used by the rest of the authorization stack. |

---

## DEFINITION OF DONE (DoD)

- [x] Canonical role/scope/action/subject definitions are settled and documented.
- [x] No duplicate or conflicting authorization primitives remain in the domain layer (old files removed, API not launched yet).
- [x] The model is ready to be consumed by the new Authorization module.
- [x] Naming is consistent with the auth/session payloads used by the application.

---

## TESTS (OPTIONAL)

| Field           | Value |
| --------------- | ----- |
| Test Type       | Unit |
| Validation      | Check that enums and permission types compile and export consistently across the backend. |
| Expected Result | A single source of truth for authorization vocabulary. |

---

## DEPENDENCIES

| Field    | Value |
| -------- | ----- |
| Tasks    | N/A |
| Epics    | EPIC_005, EPIC_001, EPIC_004 |
| External | NestJS, CASL |

---

## REFERENCES

- `backend/src/core/domain/authorization/` - Canonical location
- `backend/src/modules/authentication/domain/token.service.ts`
- `docs/specs/auth.md`
- `docs/specs/tenant.md`
- `docs/specs/user.md`

---

## TAGS

- backend
- authorization
- casl
- abac
- domain

---

## EXECUTION SUMMARY

### If DONE:

**RV Issues Fixed:**

1. **Critical: Duplicate authorization primitives** - Fixed by removing old files directly (API not launched yet):
   - Deleted `backend/src/core/domain/casl/` directory (actions.enum.ts, subjects.enum.ts)
   - Deleted `backend/src/core/domain/platform-role.enum.ts`
   - Deleted `backend/src/core/domain/tenant-role.enum.ts`
   - Deleted `backend/src/core/domain/permission-spec.ts`
   - Deleted `backend/src/core/domain/permission.types.ts`
   - Updated all consuming files to import from `@core/domain/authorization`

2. **High: Naming mismatch** - Fixed by adding `AuthScope` type alias and conversion functions:
   - Added `AuthScope` type alias: `export type AuthScope = 'platform' | 'tenant';`
   - Added `AuthorizationScopeToAuthScope()` and `AuthScopeToAuthorizationScope()` converters

3. **High: Type mismatch (string[] vs enum)** - Fixed by adding token boundary conversion layer:
   - Added `PlatformRoleHelpers.fromToken()` and `toToken()` for string[] conversion
   - Added `TenantRoleHelpers.fromToken()` and `toToken()` for string[] conversion
   - Added `toPlatformRole()`, `toPlatformRoles()`, etc. helper functions

4. **High: Structural mismatch (multi-role pattern)** - Fixed by supporting array roles in context:
   - Added `allRoles?: RoleAssignment[]` to `RequestContext`
   - Added `AuthorizationContextHelpers.fromTokenPayload()` to convert from token
   - Added `hasAnyRole()` helper for multi-role checks

5. **Medium: Missing migration path** - Fixed by cleaning up index.ts:
   - Removed migration guide referencing deleted files
   - Kept only the key concepts and usage examples

6. **Medium: Dual definition confusion** - Fixed by renaming const object:
   - Renamed `Role` const object to `RoleAssignment` factory pattern
   - Kept deprecated `Role` const for backward compatibility only

7. **Low: Pattern inconsistency** - Fixed by standardizing helper names:
   - Both `PlatformRoleHelpers` and `TenantRoleHelpers` now use `isEmpty()`
   - Deprecated `isNone()` in favor of `isEmpty()` for consistency

**Files Created/Modified:**
- `backend/src/core/domain/authorization/scope.enum.ts` - Added AuthScope alias and converters
- `backend/src/core/domain/authorization/platform-role.enum.ts` - Added token boundary conversion
- `backend/src/core/domain/authorization/tenant-role.enum.ts` - Added token boundary conversion
- `backend/src/core/domain/authorization/role.types.ts` - Added RoleAssignment factory, multi-role support
- `backend/src/core/domain/authorization/context.interface.ts` - Added fromTokenPayload, allRoles
- `backend/src/core/domain/authorization/index.ts` - All exports, migration guide removed after cleanup
- `backend/src/core/domain/casl/` - Deleted entirely
- `backend/src/core/domain/platform-role.enum.ts` - Deleted
- `backend/src/core/domain/tenant-role.enum.ts` - Deleted
- `backend/src/core/domain/permission-spec.ts` - Deleted
- `backend/src/core/domain/permission.types.ts` - Deleted

### If BLOCKED:

- Reason:
- Missing Dependency:
- Suggested Next Action:

---

## 10. NOTES

- Prefer tightening the existing model instead of adding new concepts.
- The goal is to make future ability construction straightforward, not to model every possible future policy.

---

## REVISION HISTORY

| Date       | Author | Change Summary |
| ---------- | ------ | -------------- |
| 2026-04-23 | ChatGPT | Initial version |
| 2026-04-25 | ChatGPT | Fixed RV issues: deprecated old files, added AuthScope alias, token boundary conversion, multi-role support, migration guide |
| 2026-04-25 | ChatGPT | Cleanup: removed old deprecated files directly (API not launched), migrated all imports to canonical path `@core/domain/authorization` |

---

## RV FIXES SUMMARY

### Critical Issues Fixed:
- [x] Duplicate authorization primitives - removed old files directly (API not launched yet)

### High Issues Fixed:
- [x] Naming mismatch (AuthorizationScope vs AuthScope) - added AuthScope alias
- [x] Type mismatch (string[] vs enum) - added token boundary conversion
- [x] Structural mismatch (multi-role) - added allRoles array support

### Medium Issues Fixed:
- [x] Missing migration path - added migration guide in index.ts
- [x] Dual definition confusion - renamed to RoleAssignment factory

### Low Issues Fixed:
- [x] Pattern inconsistency - standardized isEmpty() helper name