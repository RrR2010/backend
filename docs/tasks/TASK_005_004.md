---
id: TASK_005_004
title: Implement Global Authorization Guard (Fail-Closed)
type: task
status: backlog
priority: high
created: 2026-04-23
updated: 2026-04-23
tags:
  - backend
  - authorization
  - guard
  - security
  - nestjs
related:
  - EPIC_005
---

# TASK: Implement Global Authorization Guard (Fail-Closed)

---

## DESCRIPTION

- Summary (1 sentence): Add a global guard that enforces route metadata, denies missing authorization intent, and blocks unauthorized access.
- Steps (max 5):
  1. Create a global authorization guard that reads public/protected metadata from the route.
  2. Fail closed when the route is neither public nor explicitly protected.
  3. Allow explicit public routes without requiring ability evaluation.
  4. Evaluate the request ability for protected routes and return `403 Forbidden` on failure.
  5. Log denied access attempts with enough context for debugging and audit.
- Implementation hints:
  - This guard should only decide access; it should not build policies itself.
  - Use the guard to enforce the contract that every route must be explicit.
  - Missing metadata must be treated as a security error, not as a default allow.

---

## EXPECTED OUTPUT

| Field              | Value                                                                     |
| ------------------ | ------------------------------------------------------------------------- |
| Output Type        | NestJS guard                                                              |
| Output Location    | backend/src/modules/authorization/infra/                                  |
| Output Description | A global fail-closed authorization guard wired into the request pipeline. |

---

## DEFINITION OF DONE (DoD)

- [ ] Every non-public route requires explicit authorization metadata.
- [ ] Routes without metadata are denied by default.
- [ ] Public routes still work when explicitly marked.
- [ ] Protected routes return `403 Forbidden` when authorization fails.
- [ ] Denied requests are logged.
- [ ] Routes without explicit metadata are denied by default

---

## TESTS (OPTIONAL)

| Field           | Value                                                                                  |
| --------------- | -------------------------------------------------------------------------------------- |
| Test Type       | Integration                                                                            |
| Validation      | Hit public, protected, and metadata-less routes and confirm the expected status codes. |
| Expected Result | The API no longer relies only on authentication for access control.                    |

---

## DEPENDENCIES

| Field    | Value                                    |
| -------- | ---------------------------------------- |
| Tasks    | TASK_005_002, TASK_005_003, TASK_005_005 |
| Epics    | EPIC_005                                 |
| External | NestJS Guard, Reflector                  |

---

## REFERENCES

- `backend/src/main.ts`
- `backend/src/app.module.ts`
- `backend/src/modules/authentication/infra/jwt-auth.guard.ts`
- `backend/src/modules/authentication/infra/tenant-context.guard.ts`

---

## TAGS

- backend
- authorization
- guard
- security
- nestjs

---

## EXECUTION SUMMARY

### If DONE:

- Output Summary:

### If BLOCKED:

- Reason:
- Missing Dependency:
- Suggested Next Action:

---

## 10. NOTES

- This is one of the critical security tasks.
- The guard should be conservative and predictable.

---

## REVISION HISTORY

| Date       | Author  | Change Summary  |
| ---------- | ------- | --------------- |
| 2026-04-23 | ChatGPT | Initial version |
