import { Injectable } from '@nestjs/common'

@Injectable()
export class BootstrapService {
  // Business logic for bootstrap registration, webhook handling,
  // provisioning, and session claim will be implemented in subsequent phases.
}
