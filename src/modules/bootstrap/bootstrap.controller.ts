import { Controller } from '@nestjs/common'

@Controller('bootstrap')
export class BootstrapController {
  // Endpoints for registration, webhook, status, and session claim
  // will be implemented in subsequent phases.
}
